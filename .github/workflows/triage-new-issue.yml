on:
  issues:
    types: [opened]
jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: add triage label
        uses: kerhub/saved-replies@latest
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          addedLabels: 'triageee'